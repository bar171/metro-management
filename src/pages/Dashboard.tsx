/**
 * 🚧 Deprecated location — the real component is `src/features/dashboard/DashboardPage.tsx`.
 * Kept as a default-export proxy so any external import that still names this path keeps working.
 * Marked for deletion once verified.
 */
export { default } from '@/features/dashboard/DashboardPage';
