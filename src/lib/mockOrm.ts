/**
 * 🚧 Deprecated — content split across:
 *   - `src/api/mock/db/store.ts`     (in-memory tables)
 *   - `src/api/mock/db/seed.ts`      (seeders)
 *   - `src/api/mock/db/simulator.ts` (background simulation, now explicit start/stop)
 *   - `src/api/mock/services/*.mock.ts` (per-domain mock services)
 *
 * This file no longer has any consumers. Marked for deletion in the
 * cleanup step (§D of REFACTOR_PLAN.md).
 */
export {};
